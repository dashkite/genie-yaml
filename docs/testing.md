# Testing

This document outlines the approach for testing the Genie YAML preset.

## Testing approach

The testing strategy focuses on executing the preset and verifying the emitted files exist and contain the correct transformed data.

## Running tests

Use the standard Genie test command to execute the test suite.

```shell
npx genie test
```
